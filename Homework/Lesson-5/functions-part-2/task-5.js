// function createParagraph (text) {
//     let p = `<p>${text}</p>`
//     document.write(p)
// }
// createParagraph(`Hello, world!`);

const createParagraph = (text) => {
    let p = `<p>${text}</p>`
    document.write(p)
}
createParagraph(`hello, world!`);